import { Typography, Box } from '@mui/material';


const HomepageAdmin = () => {

    return (
        <Box>
            <Typography variant="h1" style={{ flexGrow: 1 }}>
                This is home page admin.
            </Typography>

        </Box>

    );
};

export default HomepageAdmin;